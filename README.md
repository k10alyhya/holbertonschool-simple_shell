# holbertonschool-simple_shell
Simple Shell is a basic UNIX command interpreter written in C. It allows users to enter commands, execute programs, handle command arguments, search for executables using `PATH`, and use basic built-in commands such as `exit` and `env`.
